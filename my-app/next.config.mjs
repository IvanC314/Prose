/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['m.media-amazon.com' , 'covers.openlibrary.org'],  // Add allowed domains for images
    },
  };
  
  export default nextConfig;
  